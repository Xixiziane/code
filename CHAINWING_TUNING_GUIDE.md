# 链翼无人机仿真测试与调参完整指南

> **适用对象**：GZ (Gazebo) SITL 仿真 + QGroundControl (QGC) 地面站
> **机型文件**：`4007_gz_chainwing`
> **模型文件**：`Tools/simulation/gz/models/chainwing/model.sdf`

---

## 目录

1. [环境准备与启动](#一环境准备与启动)
2. [测试前预检](#二测试前预检)
3. [第一阶段：地面检查（不起飞）](#三第一阶段地面检查不起飞)
4. [第二阶段：基础飞行测试](#四第二阶段基础飞行测试)
5. [第三阶段：俯仰通道调参](#五第三阶段俯仰通道调参)
6. [第四阶段：横滚通道调参](#六第四阶段横滚通道调参)
7. [第五阶段：偏航通道调参](#七第五阶段偏航通道调参)
8. [第六阶段：航向保持调参（链翼专用）](#八第六阶段航向保持调参链翼专用)
9. [第七阶段：油门与空速调参](#九第七阶段油门与空速调参)
10. [第八阶段：自主飞行测试](#十第八阶段自主飞行测试)
11. [第九阶段：降落调参](#十一第九阶段降落调参)
12. [第十阶段：RTL调参](#十二第十阶段rtl调参)
13. [所有可调参数速查表](#十三所有可调参数速查表)
14. [常见问题排查](#十四常见问题排查)
15. [日志分析方法](#十五日志分析方法)

---

## 一、环境准备与启动

### 1.1 前提条件

```bash
# 确认环境
lsb_release -a            # Ubuntu 22.04
gz sim --version           # Gazebo Garden 或 Harmonic
qgroundcontrol --version   # QGC 4.x
```

### 1.2 清理旧参数（每次大改参数后必须执行）

```bash
cd ~/PX4-Autopilot
rm -f build/px4_sitl_default/rootfs/parameters*.bson
rm -f build/px4_sitl_default/rootfs/dataman
```

### 1.3 启动仿真

```bash
# 标准启动（带GZ GUI）
make px4_sitl gz_chainwing

# WSL2 如果 GZ GUI 不出现：
export GZ_SIM_RENDER_ENGINE_GUI=ogre
make px4_sitl gz_chainwing

# 无头模式（只用QGC看遥测数据）
HEADLESS=1 make px4_sitl gz_chainwing
```

### 1.4 连接 QGC

- **原生 Ubuntu**：QGC 自动通过 UDP 14550 连接
- **WSL2**：
  ```bash
  # 在 WSL 终端查看 IP
  ip addr show eth0 | grep inet
  # QGC → 通讯连接 → 添加 → UDP → 远程主机填 WSL IP:14550
  ```

---

## 二、测试前预检

### 2.1 确认参数加载成功

在 PX4 终端（pxh>）输入：

```bash
# 确认机架号
param show SYS_AUTOSTART
# 应显示: 4007

# 确认关键参数已加载
param show FW_YAW_STAB_SC
# 应显示: 2.0000

param show CA_ROTOR_COUNT
# 应显示: 3

param show FD_ESCS_EN
# 应显示: 0
```

### 2.2 确认传感器正常

```bash
# 检查 IMU 数据是否正常（SITL 中使用 listener 命令查看话题数据）
# 注意：sensor_accel_fifo 和 sensor_gyro_fifo 是 uORB 话题名，不是模块名
# 不能用 "sensor_accel_fifo status"，应该用 listener 命令
listener sensor_accel_fifo -n 1
# 应显示：有加速度数据，z轴约 -9.8 m/s²

listener sensor_gyro_fifo -n 1
# 应显示：有角速度数据，静止时接近 0

# 检查空速（地面静止时可能显示负值，这是正常的）
listener airspeed -n 1
# 地面时: indicated_airspeed_m_s 可能为 -1~+1 m/s（噪声）
#          confidence: 0.00000（不可信，因为在地面）
# 起飞后: indicated_airspeed_m_s 应为 15-25 m/s
#          confidence: 1.00000（可信）

# 检查 EKF2 状态
ekf2 status
# 应显示: attitude: 1, local position: 1, global position: 1
# "airspeed messages missed: XX" 在启动初期是正常的启动时序现象
```

### 2.3 确认执行器映射

```bash
# 查看电机映射
param show SIM_GZ_EC_FUNC*
# 应显示: FUNC1=101, FUNC2=102, FUNC3=103

# 查看舵面映射
param show SIM_GZ_SV_FUNC*
# 应显示: FUNC1=201, FUNC2=202, FUNC3=203
```

### 2.4 等待 "Ready for takeoff"

```
INFO [commander] Ready for takeoff!
```

如果没有出现，检查 PX4 终端是否有 `WARN` 或 `Preflight Fail` 消息。

---

## 三、第一阶段：地面检查（不起飞）

### 3.1 舵面响应测试

**目的**：确认舵面偏转方向正确

1. 在 QGC → 飞行器设置 → 执行器 (Actuators) 页面
2. 手动滑动各通道滑块，观察 GZ 中舵面偏转

| 测试 | 操作 | 预期响应 |
|------|------|----------|
| 俯仰上 | 增大 Pitch 通道 | 所有3个舵面向上偏转（机头抬） |
| 俯仰下 | 减小 Pitch 通道 | 所有3个舵面向下偏转（机头低） |
| 横滚右 | 增大 Roll 通道 | 左升降副翼向上，右升降副翼向下 |
| 横滚左 | 减小 Roll 通道 | 左升降副翼向下，右升降副翼向上 |

**如果方向反了**：
```bash
# 反转横滚方向
param set CA_SV_CS0_TRQ_R -0.5   # 原来是 +0.5
param set CA_SV_CS2_TRQ_R 0.5    # 原来是 -0.5
```

### 3.2 电机响应测试

1. 在 QGC → 执行器页面，手动测试各电机
2. 每次只开一个电机，确认对应的螺旋桨旋转

| 测试 | 操作 | 预期响应 |
|------|------|----------|
| 电机1 | 滑动 Motor 1 | 左侧螺旋桨旋转 |
| 电机2 | 滑动 Motor 2 | 中间螺旋桨旋转 |
| 电机3 | 滑动 Motor 3 | 右侧螺旋桨旋转 |

### 3.3 差动推力偏航测试

1. 同时开三个电机到中等推力
2. 施加偏航指令

| 测试 | 操作 | 预期响应 |
|------|------|----------|
| 偏航右 | 增大 Yaw 通道 | 右电机减速，左电机加速 |
| 偏航左 | 减小 Yaw 通道 | 左电机减速，右电机加速 |

---

## 四、第二阶段：基础飞行测试

### 4.1 手动起飞

```bash
# PX4 终端
commander arm                    # 解锁
commander mode stabilized        # 切换到增稳模式
```

然后用 QGC 的虚拟摇杆（或连接遥控器）手动推油门起飞。

### 4.2 自动起飞

```bash
# PX4 终端
commander takeoff
```

### 4.3 观察指标

在 QGC → 分析 (Analyze) 页面，添加以下实时曲线：

| Topic | 字段 | 观察目的 |
|-------|------|----------|
| `vehicle_attitude` | roll, pitch, yaw | 姿态角是否稳定 |
| `vehicle_angular_velocity` | xyz | 角速率是否平滑 |
| `airspeed_validated` | calibrated_airspeed_m_s | 空速是否在范围内 |
| `actuator_outputs` | output[0..2] | 电机输出是否合理 |
| `actuator_outputs` | output[3..5] | 舵面偏转是否合理 |
| `vehicle_local_position` | z | 高度变化 |

### 4.4 判断基础飞行是否正常

| 指标 | 正常范围 | 异常表现 |
|------|----------|----------|
| 横滚角 | ±5° 内微小波动 | 持续偏向一侧或大幅振荡 |
| 俯仰角 | 2°~8° 附近（抬头） | 持续拉大仰角或低头 |
| 偏航角 | 缓慢漂移 < 2°/s | 持续高速旋转 |
| 空速 | 15~25 m/s | 持续增加或下降到失速 |
| 高度 | 稳定在目标高度 ±5m | 持续上升或下降 |

---

## 五、第三阶段：俯仰通道调参

### 5.1 调参顺序（由内到外）

**先调内环（角速率控制器），再调外环（姿态控制器）**

### 5.2 内环：俯仰角速率 PID

| 参数 | 当前值 | 作用 | 调参方法 |
|------|--------|------|----------|
| `FW_PR_P` | 0.9 | 比例增益 | 先调此项。增大→响应更快但可能振荡；减小→响应慢 |
| `FW_PR_I` | 0.5 | 积分增益 | 消除稳态误差。增大→配平更准但可能积分饱和；减小→允许小偏差 |
| `FW_PR_FF` | 0.5 | 前馈增益 | 提前补偿。增大→响应更快；过大→超调 |

**调参步骤**：

```bash
# 步骤1: 先将 I 和 FF 设为 0，只调 P
param set FW_PR_I 0
param set FW_PR_FF 0

# 步骤2: 从 P=0.3 开始，逐步增大
param set FW_PR_P 0.3

# 步骤3: 在QGC中观察 vehicle_angular_velocity.y (俯仰角速率)
# 给一个俯仰阶跃输入（推杆再放手），观察：
# - 响应太慢 → 增大 P（+0.1）
# - 开始振荡 → 减小 P（-0.1）
# 目标：快速响应、无振荡

# 步骤4: 找到临界 P 值后，设为 70% 的临界值
# 例如 P=1.2 开始振荡 → 设 P=0.84

# 步骤5: 加入 FF
param set FW_PR_FF 0.3
# 增大直到响应明显改善但不超调

# 步骤6: 加入 I
param set FW_PR_I 0.2
# 增大直到稳态误差消除，但不要太大（避免积分饱和）
```

### 5.3 外环：俯仰姿态控制器

| 参数 | 当前值 | 作用 | 调参方法 |
|------|--------|------|----------|
| `FW_P_TC` | 0.5 | 时间常数 (秒) | 减小→响应更快；增大→响应更慢更平滑 |
| `FW_P_RMAX_POS` | 25.0 | 最大抬头速率 (°/s) | 限制最大俯仰速率 |
| `FW_P_RMAX_NEG` | 25.0 | 最大低头速率 (°/s) | 限制最大低头速率 |
| `FW_PSP_OFF` | 2.0 | 俯仰角偏移 (°) | 水平飞行时的默认俯仰角 |

**调参步骤**：

```bash
# 步骤1: 观察水平飞行时俯仰角是否稳定在 FW_PSP_OFF 附近
# 如果飞机持续抬头 → 减小 FW_PSP_OFF
param set FW_PSP_OFF 1.0

# 如果飞机持续低头 → 增大 FW_PSP_OFF
param set FW_PSP_OFF 3.0

# 步骤2: 测试俯仰响应速度
# 推杆到 -10°，观察实际俯仰角跟踪速度
# 如果太慢 → 减小 FW_P_TC
param set FW_P_TC 0.4

# 如果振荡 → 增大 FW_P_TC
param set FW_P_TC 0.6
```

### 5.4 俯仰配平

```bash
# 如果舵面持续偏转（积分器一直在补偿）
# 调整机械配平
param set TRIM_PITCH -0.20   # 更多低头配平
# 或
param set TRIM_PITCH -0.10   # 更少低头配平

# 目标：在巡航速度水平飞行时，升降舵接近零位
```

### 5.5 俯仰调参通过标准

- [ ] 水平飞行时俯仰角稳定在 FW_PSP_OFF ±2° 以内
- [ ] 俯仰阶跃响应无超调或只有轻微超调（<20%）
- [ ] 升降舵在巡航时接近零位（配平正确）
- [ ] 无俯仰振荡

---

## 六、第四阶段：横滚通道调参

### 6.1 内环：横滚角速率 PID

| 参数 | 当前值 | 作用 | 调参方法 |
|------|--------|------|----------|
| `FW_RR_P` | 0.3 | 比例增益 | 先调此项 |
| `FW_RR_I` | 0.5 | 积分增益 | 消除稳态偏差 |
| `FW_RR_FF` | 0.5 | 前馈增益 | 提前补偿 |

**链翼特殊注意**：由于升降副翼是反向配置的，横滚增益不宜过大，否则可能激发各飞行单元间的相对运动。

**调参步骤**：

```bash
# 步骤1: 从低 P 值开始
param set FW_RR_I 0
param set FW_RR_FF 0
param set FW_RR_P 0.15

# 步骤2: 在增稳模式下轻推横滚摇杆，然后放手
# 观察 vehicle_angular_velocity.x (横滚角速率)
# - 响应太慢 → 增大 P (+0.05)
# - 开始振荡（角速率来回抖动）→ 减小 P (-0.05)

# 步骤3: 找到不振荡的最大 P 后，加入 FF
param set FW_RR_FF 0.3

# 步骤4: 加入 I（链翼建议不要太大）
param set FW_RR_I 0.1
# 链翼横滚I值建议 ≤ 0.2，防止各单元之间的耦合导致积分缠绕
```

### 6.2 外环：横滚姿态控制器

| 参数 | 当前值 | 作用 |
|------|--------|------|
| `FW_R_TC` | 0.5 | 横滚时间常数 |
| `FW_R_RMAX` | 30.0 | 最大横滚速率 (°/s) |

```bash
# 链翼建议限制横滚速率，避免翼尖连接处过载
param set FW_R_RMAX 20   # 更保守的横滚速率

# 如果横滚响应太慢
param set FW_R_TC 0.4     # 减小时间常数
```

### 6.3 横滚调参通过标准

- [ ] 水平飞行时横滚角在 ±3° 以内
- [ ] 横滚阶跃响应快速且无振荡
- [ ] 松开摇杆后飞机自动回到零横滚（±2° 以内）
- [ ] 无横滚轴高频振荡（区别于偏航耦合的低频振荡）

---

## 七、第五阶段：偏航通道调参

### 7.1 内环：偏航角速率 PID

| 参数 | 当前值 | 作用 | 调参方法 |
|------|--------|------|----------|
| `FW_YR_P` | 0.6 | 比例增益 | 差动推力偏航响应 |
| `FW_YR_I` | 0.5 | 积分增益 | 消除稳态偏航偏差 |
| `FW_YR_FF` | 0.5 | 前馈增益 | 协调转弯前馈 |

**链翼特殊注意**：差动推力同时产生偏航和横滚力矩（因为电机在 Y=±1.2m 处），YR_P 过大会引起横滚-偏航耦合振荡。

**调参步骤**：

```bash
# 步骤1: 从低值开始
param set FW_YR_I 0
param set FW_YR_FF 0
param set FW_YR_P 0.2

# 步骤2: 在 QGC 中观察 vehicle_angular_velocity.z (偏航角速率)
# 直线飞行时应接近0
# 如果偏航角速率持续不为0 → 增大 P（+0.1）
# 如果出现横滚-偏航来回振荡 → 减小 P（-0.1）

# 步骤3: 加入 FF
param set FW_YR_FF 0.3
# 转弯时观察是否协调（无侧滑感觉）

# 步骤4: 加入 I
param set FW_YR_I 0.1
# 链翼建议 I ≤ 0.3，防止差动推力导致横滚积分缠绕
```

### 7.2 偏航速率限制

```bash
# 链翼偏航速率不宜过快（惯量大）
param set FW_Y_RMAX 10     # 最大偏航速率 10°/s（原15）
# 如果转弯太慢 → 增大到 15
# 如果仍然振荡 → 减小到 8
```

### 7.3 偏航调参通过标准

- [ ] 直线飞行时偏航角速率 < 2°/s
- [ ] 协调转弯时无明显侧滑
- [ ] 无横滚-偏航耦合振荡（低频左右摇摆）
- [ ] 差动推力输出平滑（电机输出不跳变）

---

## 八、第六阶段：航向保持调参（链翼专用）

### 8.1 航向保持增益

| 参数 | 当前值 | 作用 | 范围 |
|------|--------|------|------|
| `FW_YAW_STAB_SC` | 2.0 | 航向保持P增益 | 0.0~5.0 |

**调参步骤**：

```bash
# 步骤1: 确认航向保持已启用
param show FW_YAW_STAB_SC
# 应显示 > 0

# 步骤2: 在增稳模式下进入直线飞行
# 松开所有摇杆，观察航向是否保持

# 步骤3: 如果航向缓慢漂移（> 5°/分钟）
# → 增大增益
param set FW_YAW_STAB_SC 3.0

# 步骤4: 如果飞机左右摇摆（航向振荡）
# → 减小增益
param set FW_YAW_STAB_SC 1.0

# 步骤5: 精细调整
# 目标：航向保持在 ±2° 以内，无振荡
```

### 8.2 航向保持 vs 偏航速率增益的关系

`FW_YAW_STAB_SC` 和 `FW_YR_P` 共同决定偏航响应：

```
总偏航速率 = 协调转弯速率 + FW_YAW_STAB_SC × heading_error × (V/V_trim)²
然后：
偏航力矩 = FW_YR_P × (目标偏航速率 - 实际偏航速率) + FW_YR_I × ∫误差 + FW_YR_FF × 前馈
```

**调参原则**：
- `FW_YAW_STAB_SC` 控制"多大的航向偏差产生多大的偏航速率指令"
- `FW_YR_P` 控制"偏航速率指令如何转化为差动推力输出"
- 如果两者乘积太大，会导致偏航-横滚耦合振荡

**推荐组合**：

| FW_YAW_STAB_SC | FW_YR_P | 效果 |
|-----------------|---------|------|
| 1.0 | 0.6 | 温和保持，允许少量漂移 |
| 2.0 | 0.4 | 平衡选择 |
| 3.0 | 0.3 | 积极保持，可能有轻微振荡 |
| 2.0 | 0.6 | 当前设置，可能过于激进 |

### 8.3 航向保持调参通过标准

- [ ] 增稳模式松开摇杆：航向保持在 ±3° 以内
- [ ] 推偏航摇杆：飞机按预期方向转弯
- [ ] 松开偏航摇杆：飞机锁定在新航向
- [ ] 无持续的偏航-横滚低频振荡（周期 > 2秒的摇摆）

---

## 九、第七阶段：油门与空速调参

### 9.1 空速参数

| 参数 | 当前值 | 作用 | 调参方法 |
|------|--------|------|----------|
| `FW_AIRSPD_TRIM` | 20 m/s | 巡航空速 | 来自 MATLAB V0=20 |
| `FW_AIRSPD_MIN` | 15 m/s | 最小安全空速 | 应 > 失速速度×1.3 |
| `FW_AIRSPD_MAX` | 30 m/s | 最大空速 | 结构限制 |
| `FW_AIRSPD_STALL` | 8 m/s | 失速速度 | 来自 MATLAB 数据 |

```bash
# 测试步骤：
# 1. 起飞后进入水平巡航
# 2. 在 QGC 中观察 airspeed_validated.calibrated_airspeed_m_s
# 3. 如果巡航速度偏离 FW_AIRSPD_TRIM：

# 速度持续偏高 → 减小油门配平
param set FW_THR_TRIM 0.50

# 速度持续偏低 → 增大油门配平
param set FW_THR_TRIM 0.70
```

### 9.2 油门参数

| 参数 | 当前值 | 作用 |
|------|--------|------|
| `FW_THR_TRIM` | 0.60 | 配平油门（巡航所需） |
| `FW_THR_MIN` | 0.05 | 最小油门（防止电机完全停转） |
| `FW_THR_MAX` | 1.0 | 最大油门 |

```bash
# 实际巡航时的目标：
# - 油门输出应在 FW_THR_TRIM ±10% 范围内波动
# - 如果油门持续偏高 → 飞机阻力比预期大，或 TRIM 设太低
# - 如果油门持续偏低 → 飞机阻力比预期小，或 TRIM 设太高
```

### 9.3 TECS（总能量控制系统）参数

| 参数 | 当前值 | 作用 |
|------|--------|------|
| `FW_T_CLMB_MAX` | 8 m/s | 最大爬升率 |
| `FW_T_SINK_MAX` | 3.0 m/s | 最大下降率 |
| `FW_T_SINK_MIN` | 2.5 m/s | 最小下降率 |
| `FW_P_LIM_MAX` | 15° | 最大仰角 |
| `FW_P_LIM_MIN` | -15° | 最大俯角 |

```bash
# 爬升测试：
# commander takeoff → 观察爬升率
# 如果爬升时速度下降到 FW_AIRSPD_MIN 以下 → 减小 FW_T_CLMB_MAX
param set FW_T_CLMB_MAX 5

# 如果爬升太慢 → 增大 FW_T_CLMB_MAX
param set FW_T_CLMB_MAX 10
```

### 9.4 油门空速调参通过标准

- [ ] 巡航时空速稳定在 FW_AIRSPD_TRIM ±2 m/s
- [ ] 巡航时油门在 FW_THR_TRIM ±10% 范围内
- [ ] 爬升时空速不低于 FW_AIRSPD_MIN
- [ ] 下降时空速不超过 FW_AIRSPD_MAX

---

## 十、第八阶段：自主飞行测试

### 10.1 Loiter（盘旋）测试

```bash
# 起飞后进入盘旋模式
commander takeoff
# 等待到达高度后：
commander mode loiter
```

**观察**：
- 盘旋半径是否接近 `NAV_LOITER_RAD` (50m)
- 高度是否保持稳定
- 是否有航向振荡

```bash
# 如果盘旋半径太大 → 飞机转弯能力不足
param set NAV_LOITER_RAD 60   # 增大允许半径

# 如果盘旋时高度波动大
# → 检查俯仰/油门控制是否调好
```

### 10.2 Waypoint（航点）任务测试

1. 在 QGC → 规划 (Plan) 页面创建简单任务：
   - 起飞航点
   - 3-4个直线飞行航点（高度30m）
   - 降落航点

2. 上传并执行：
   ```bash
   commander mode auto:mission
   commander arm
   ```

**观察**：
- 航点跟踪精度（偏离航线距离）
- 转弯时的姿态变化
- 航点间直线飞行的稳定性

```bash
# 如果航线跟踪不准（偏离 > 30m）
param set NPFG_PERIOD 15   # 增大导航周期（更平滑但反应慢）
# 或
param set NPFG_PERIOD 10   # 减小导航周期（更灵敏但可能振荡）

# 如果航点接受半径太小导致飞不到
param set NAV_ACC_RAD 30   # 增大接受半径
```

### 10.3 自主飞行调参通过标准

- [ ] 盘旋时高度保持 ±5m 以内
- [ ] 盘旋时不偏航振荡
- [ ] 航点间直线飞行偏离 < 20m
- [ ] 转弯平滑，无剧烈姿态变化

---

## 十一、第九阶段：降落调参

### 11.1 降落参数

| 参数 | 当前值 | 作用 |
|------|--------|------|
| `FW_LND_ANG` | 8° | 降落接近角 |
| `FW_LND_AIRSPD` | 16 m/s | 降落接近速度 |
| `FW_LND_FLALT` | 5 m | 拉平高度 |
| `FW_LND_FL_PMIN` | 3° | 拉平最小俯仰角 |
| `FW_LND_FL_PMAX` | 10° | 拉平最大俯仰角 |

### 11.2 降落测试步骤

```bash
# 方法1: 直接降落命令
commander land

# 方法2: 通过QGC规划包含降落航点的任务
```

**观察**：
- 接近阶段：速度是否保持在 `FW_LND_AIRSPD` 附近
- 接近阶段：下滑角是否合理
- 拉平阶段：是否平滑过渡到平飞
- 着地：是否稳定（不弹跳、不打转）

```bash
# 如果降落时打转（低速偏航发散）
# → 这是 FW_YAW_STAB_SC 在低速时的效果
# 空速依赖增益缩放已自动处理，但如果仍然打转：
param set FW_YAW_STAB_SC 1.0   # 进一步降低航向保持增益

# 如果降落时速度太低（接近失速）
param set FW_LND_AIRSPD 18     # 增大接近速度

# 如果降落角度太陡
param set FW_LND_ANG 5         # 减小接近角
```

### 11.3 降落调参通过标准

- [ ] 接近阶段速度稳定（不低于失速×1.3）
- [ ] 接近阶段航向稳定（不打转）
- [ ] 拉平过渡平滑
- [ ] 着地时无大幅弹跳

---

## 十二、第十阶段：RTL调参

### 12.1 RTL 参数

| 参数 | 当前值 | 作用 |
|------|--------|------|
| `RTL_RETURN_ALT` | 30 m | 返航高度 |
| `RTL_DESCEND_ALT` | 15 m | 返航后下降到的高度 |
| `RTL_LAND_DELAY` | 0 秒 | 到达后等待降落时间（0=立即） |

### 12.2 RTL 测试

```bash
# 起飞后飞离起飞点一段距离（> 100m）
commander takeoff
# 等待...
commander rtl
```

**观察**：
- 飞机是否转向起飞点
- 是否爬升到 `RTL_RETURN_ALT` (30m)
- 到达起飞点上空后是否下降到 `RTL_DESCEND_ALT` (15m)
- 是否自动进入降落程序

```bash
# 如果不想RTL时自动降落
param set RTL_LAND_DELAY -1   # 永不自动降落（需手动commander land）

# 如果RTL高度太低（可能撞地形）
param set RTL_RETURN_ALT 50
```

---

## 十三、所有可调参数速查表

### 链翼核心参数（我们新增的）

| 参数 | 默认值 | 链翼设置 | 说明 |
|------|--------|----------|------|
| `FW_YAW_STAB_SC` | 0.0 | 2.0 | 航向保持增益。0=关闭 |

### 姿态控制器

| 参数 | 当前值 | 建议范围 | 说明 |
|------|--------|----------|------|
| `FW_R_TC` | 0.5 | 0.3~0.8 | 横滚时间常数 |
| `FW_P_TC` | 0.5 | 0.3~0.8 | 俯仰时间常数 |
| `FW_R_RMAX` | 30 | 15~60 | 最大横滚速率 °/s |
| `FW_P_RMAX_POS` | 25 | 10~30 | 最大抬头速率 °/s |
| `FW_P_RMAX_NEG` | 25 | 10~30 | 最大低头速率 °/s |
| `FW_Y_RMAX` | 15 | 5~20 | 最大偏航速率 °/s |
| `FW_PSP_OFF` | 2.0 | 0~8 | 俯仰角偏移 ° |

### 角速率控制器（内环 PID）

| 参数 | 当前值 | 建议范围 | 说明 |
|------|--------|----------|------|
| `FW_PR_P` | 0.9 | 0.3~1.5 | 俯仰速率 P |
| `FW_PR_I` | 0.5 | 0.1~1.0 | 俯仰速率 I |
| `FW_PR_FF` | 0.5 | 0.2~0.8 | 俯仰速率 FF |
| `FW_RR_P` | 0.3 | 0.1~0.8 | 横滚速率 P |
| `FW_RR_I` | 0.5 | 0.05~0.5 | 横滚速率 I |
| `FW_RR_FF` | 0.5 | 0.2~0.8 | 横滚速率 FF |
| `FW_YR_P` | 0.6 | 0.1~1.0 | 偏航速率 P |
| `FW_YR_I` | 0.5 | 0.05~0.5 | 偏航速率 I |
| `FW_YR_FF` | 0.5 | 0.2~0.8 | 偏航速率 FF |

### 空速与油门

| 参数 | 当前值 | 建议范围 | 说明 |
|------|--------|----------|------|
| `FW_AIRSPD_TRIM` | 20 | 15~25 | 巡航空速 m/s |
| `FW_AIRSPD_MIN` | 15 | 10~18 | 最小空速 m/s |
| `FW_AIRSPD_MAX` | 30 | 25~40 | 最大空速 m/s |
| `FW_AIRSPD_STALL` | 8 | 5~12 | 失速速度 m/s |
| `FW_THR_TRIM` | 0.60 | 0.3~0.8 | 配平油门 |
| `FW_THR_MIN` | 0.05 | 0~0.15 | 最小油门 |
| `FW_THR_MAX` | 1.0 | 0.8~1.0 | 最大油门 |

### TECS 与性能

| 参数 | 当前值 | 建议范围 | 说明 |
|------|--------|----------|------|
| `FW_T_CLMB_MAX` | 8 | 3~15 | 最大爬升率 m/s |
| `FW_T_SINK_MAX` | 3.0 | 2~5 | 最大下降率 m/s |
| `FW_T_SINK_MIN` | 2.5 | 1~3 | 最小下降率 m/s |
| `FW_P_LIM_MAX` | 15 | 10~25 | 最大仰角 ° |
| `FW_P_LIM_MIN` | -15 | -25~-5 | 最大俯角 ° |

### 导航

| 参数 | 当前值 | 建议范围 | 说明 |
|------|--------|----------|------|
| `NAV_LOITER_RAD` | 50 | 30~100 | 盘旋半径 m |
| `NAV_ACC_RAD` | 20 | 10~50 | 航点接受半径 m |
| `NPFG_PERIOD` | 12 | 8~20 | 导航周期 s |

### 降落

| 参数 | 当前值 | 建议范围 | 说明 |
|------|--------|----------|------|
| `FW_LND_ANG` | 8 | 3~10 | 降落接近角 ° |
| `FW_LND_AIRSPD` | 16 | 12~20 | 降落空速 m/s |
| `FW_LND_FLALT` | 5 | 3~10 | 拉平高度 m |
| `FW_LND_FL_PMIN` | 3 | 1~5 | 拉平最小俯仰 ° |
| `FW_LND_FL_PMAX` | 10 | 5~15 | 拉平最大俯仰 ° |

### 任务与RTL

| 参数 | 当前值 | 建议范围 | 说明 |
|------|--------|----------|------|
| `MIS_TAKEOFF_ALT` | 15 | 10~30 | 起飞目标高度 m |
| `RTL_RETURN_ALT` | 30 | 20~100 | 返航高度 m |
| `RTL_DESCEND_ALT` | 15 | 10~50 | 返航下降高度 m |
| `RTL_LAND_DELAY` | 0 | -1~30 | 返航后等待降落 s（-1=永不） |

### 配平

| 参数 | 当前值 | 建议范围 | 说明 |
|------|--------|----------|------|
| `TRIM_PITCH` | -0.15 | -0.3~0 | 俯仰舵面配平 |

### EKF2 传感器噪声

| 参数 | 当前值 | 建议范围 | 说明 |
|------|--------|----------|------|
| `EKF2_ACC_NOISE` | 0.5 | 0.3~1.0 | 加速度计噪声 |
| `EKF2_GYR_NOISE` | 0.02 | 0.01~0.05 | 陀螺仪噪声 |
| `EKF2_ACC_B_NOISE` | 0.005 | 0.001~0.01 | 加速度计偏置噪声 |

### 控制分配

| 参数 | 当前值 | 说明 |
|------|--------|------|
| `CA_ROTOR0_PY` | -1.2 | 左电机 Y 位置 m |
| `CA_ROTOR1_PY` | 0.0 | 中电机 Y 位置 m |
| `CA_ROTOR2_PY` | 1.2 | 右电机 Y 位置 m |
| `CA_SV_CS0_TRQ_R` | 0.5 | 左升降副翼横滚效能 |
| `CA_SV_CS2_TRQ_R` | -0.5 | 右升降副翼横滚效能 |

---

## 十四、常见问题排查

| 现象 | 可能原因 | 解决方法 |
|------|----------|----------|
| 飞机地面打转不起飞 | 电机反扭矩不平衡 | 检查 model.sdf 中电机 direction 配置 |
| 起飞后立即解锁失败 | FD_ESCS_EN=1 | `param set FD_ESCS_EN 0` |
| 直线飞行时航向缓慢漂移 | FW_YAW_STAB_SC 太小 | 增大到 2.0~3.0 |
| 飞行中偏航-横滚振荡 | FW_YAW_STAB_SC×FW_YR_P 太大 | 降低其中一个 |
| 降落时打转 | 低速偏航增益过大 | 降低 FW_YAW_STAB_SC 到 1.0 |
| 巡航时俯仰振荡 | FW_PR_P 太大 | 减小 FW_PR_P |
| 速度持续下降 | FW_THR_TRIM 太低 | 增大 FW_THR_TRIM |
| 爬升时速度不足 | FW_T_CLMB_MAX 太大 | 减小 FW_T_CLMB_MAX |
| RTL 不降落 | RTL_LAND_DELAY=-1 | `param set RTL_LAND_DELAY 0` |
| QGC 提示缺少参数 | 参数缓存残留 | 删除 parameters.bson 重启 |

---

## 十五、日志分析方法

### 15.1 下载日志

```bash
# 日志保存在 PX4 SITL 目录下：
ls build/px4_sitl_default/rootfs/log/
# 或在 QGC → 分析工具 → 日志下载
```

### 15.2 使用 Flight Review

1. 打开 https://review.px4.io/
2. 上传 `.ulg` 日志文件
3. 查看以下关键图表：

| 图表 | 查看内容 |
|------|----------|
| Roll/Pitch/Yaw | 姿态跟踪是否准确 |
| Roll/Pitch/Yaw Rate | 角速率是否平滑 |
| Airspeed | 空速是否在安全范围 |
| Thrust | 油门输出是否合理 |
| Actuator Controls | 舵面/电机输出 |
| Vibration | 振动水平（GZ 中应很低） |

### 15.3 使用 PlotJuggler

```bash
# 安装
sudo apt install ros-humble-plotjuggler-ros
# 打开 .ulg 文件进行详细分析
```

### 15.4 重点关注的信号

| 信号 | 正常特征 | 异常特征 |
|------|----------|----------|
| roll_rate setpoint vs actual | 相位差 < 100ms | 持续滞后或振荡 |
| pitch_rate setpoint vs actual | 跟踪紧密 | 大幅偏差 |
| yaw_rate setpoint vs actual | 跟踪紧密 | 持续偏差或发散 |
| motor_output[0]-[2] | 差值 < 0.3 | 差值持续 > 0.5（差动推力饱和） |
| control_surface[0]-[2] | 配平附近小幅波动 | 持续偏向极值（执行器饱和） |

---

## 调参完成检查清单

完成所有阶段后，确认以下全部通过：

- [ ] **地面检查**：舵面方向正确，电机映射正确
- [ ] **基础飞行**：起飞后飞机可控，不打转不翻转
- [ ] **俯仰**：水平飞行俯仰稳定，无振荡
- [ ] **横滚**：水平飞行横滚在 ±3° 以内，响应快速
- [ ] **偏航**：直线飞行航向稳定，无低频振荡
- [ ] **航向保持**：松开摇杆航向不漂移
- [ ] **空速**：巡航空速在 TRIM ±2 m/s
- [ ] **盘旋**：高度保持，航向无振荡
- [ ] **航点**：航线跟踪偏差 < 20m
- [ ] **降落**：平稳接地，不打转
- [ ] **RTL**：自动返航并降落
